#Makefile.setup.platform for 70D

ifeq ($(FW_VERSION),112)
CANON_NAME_FIR=70D00112.FIR
ROMBASEADDR     = 0xFF0C0000

ifeq ($(ML_SRC_PROFILE),generic)
# DryOSmemory map
# RESTARTSTART is selected to be just above the end of the bss
# BSS_END 000FAE30 for new ICU and 000FAE50 for old ICU
RESTARTSTART    = 0x00C1C100
else
RESTARTSTART    = 0x000FAF00
endif

#End of FW_VERSION == 112
endif
